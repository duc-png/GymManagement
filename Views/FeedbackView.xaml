<UserControl x:Class="GymManagement.Views.FeedbackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Text="Tất cả đánh giá" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="SearchTextBox" Width="280" Height="32" VerticalContentAlignment="Center" Padding="8,0" TextChanged="SearchTextBox_TextChanged"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="MyFeedbackButton" Content="Đánh giá về tôi" Width="120" Height="32" Click="MyFeedbackButton_Click"/>
                <Button Content="Tất cả đánh giá" Width="120" Height="32" Margin="8,0,0,0" Click="AllFeedbackButton_Click"/>
            </StackPanel>
        </Grid>

        <DataGrid x:Name="FeedbackGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hội viên" Width="170" Binding="{Binding MemberName}"/>
                <DataGridTextColumn Header="Đối tượng" Width="180" Binding="{Binding TargetName}"/>
                <DataGridTextColumn Header="Loại" Width="140" Binding="{Binding TypeName}"/>
                <DataGridTextColumn Header="Số sao" Width="150" Binding="{Binding RatingText}"/>
                <DataGridTemplateColumn Header="Nội dung" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Comment}" TextWrapping="Wrap" Margin="4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Ngày gửi" Width="135" Binding="{Binding SubmittedDate, StringFormat=dd/MM/yyyy HH:mm}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
