<UserControl x:Class="GymManagement.Views.PtPortfolioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:GymManagement.Converters">
    <UserControl.Resources>
        <converters:AvatarPathConverter x:Key="AvatarPathConverter"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="PT Portfolio" FontSize="28" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="Available personal trainers and their specialties" Foreground="#666" Margin="0,8,0,20"/>
        <ListView x:Name="PtList" Grid.Row="2" BorderThickness="0">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate><UniformGrid Columns="3"/></ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" BorderBrush="#DDD" BorderThickness="1" Margin="8" Padding="16" CornerRadius="6">
                        <StackPanel>
                            <Grid Width="84" Height="84" HorizontalAlignment="Left">
                                <Ellipse Fill="#E5E7EB"/>
                                <TextBlock Text="PT" Foreground="#6B7280" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{Binding Avatar, Converter={StaticResource AvatarPathConverter}}" Stretch="UniformToFill"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Grid>
                            <TextBlock Text="{Binding FullName}" FontSize="20" FontWeight="SemiBold" Margin="0,12,0,0"/>
                            <TextBlock Text="{Binding Specialty, TargetNullValue=Not specified}" Foreground="#E63946" Margin="0,6,0,0"/>
                            <TextBlock Text="{Binding Ptstatus, TargetNullValue=Status unavailable}" Foreground="#666" Margin="0,6,0,0"/>
                            <TextBlock Text="Media: {Binding Ptmedia.Count}" Foreground="#666" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
