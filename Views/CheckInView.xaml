<UserControl x:Class="GymManagement.Views.CheckInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Check-in / Check-out" FontSize="28" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="Enter the member code to simulate a QR scan." Foreground="#666" Margin="0,8,0,24"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBox x:Name="MemberCodeTextBox" Width="300" Height="38" Padding="10,0" VerticalContentAlignment="Center"/>
            <Button Content="Scan" Width="100" Height="38" Margin="10,0,0,0" Click="ScanButton_Click"/>
        </StackPanel>
        <Border x:Name="ResultBorder" Grid.Row="3" Margin="0,30,0,0" Padding="20" Background="#F3F4F6" CornerRadius="8" Height="90">
            <TextBlock x:Name="ResultText" FontSize="20" VerticalAlignment="Center" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>
