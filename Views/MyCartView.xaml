<UserControl x:Class="GymManagement.Views.MyCartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Giỏ hàng của tôi" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <WrapPanel>
                <TextBlock Text="Gói tập:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="PackageComboBox" Width="250" DisplayMemberPath="PackageName" SelectedValuePath="Id"/>
                <Button Content="Thêm gói" Width="95" Margin="8,0,20,0" Click="AddPackageButton_Click"/>
                <TextBlock Text="Lịch PT mua thêm:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="BookingComboBox" Width="240" DisplayMemberPath="StartTime" SelectedValuePath="Id"/>
                <Button Content="Thêm PT" Width="90" Margin="8,0,0,0" Click="AddBookingButton_Click"/>
            </WrapPanel>
            <WrapPanel Grid.Row="1" Margin="0,10,0,0">
                <TextBlock Text="Sản phẩm:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="ProductComboBox" Width="250" DisplayMemberPath="ProductName" SelectedValuePath="Id"/>
                <TextBlock Text="Số lượng:" VerticalAlignment="Center" Margin="12,0,8,0"/>
                <TextBox x:Name="ProductQuantityTextBox" Text="1" Width="60" Padding="5,3"/>
                <Button Content="Thêm sản phẩm" Width="120" Margin="8,0,20,0" Click="AddProductButton_Click"/>
                <Button Content="Xóa mục đã chọn" Width="130" Click="RemoveButton_Click"/>
            </WrapPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <DataGrid x:Name="CartGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,0,12,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên" Width="*" Binding="{Binding ItemName}"/>
                    <DataGridTextColumn Header="Loại" Width="100" Binding="{Binding ItemType}"/>
                    <DataGridTextColumn Header="Số lượng" Width="90" Binding="{Binding Quantity}"/>
                    <DataGridTextColumn Header="Đơn giá" Width="140" Binding="{Binding UnitPrice, StringFormat={}{0:N0} đ}"/>
                </DataGrid.Columns>
            </DataGrid>
            <Border Grid.Column="1" BorderBrush="#DDDDDD" BorderThickness="1" Padding="16">
                <StackPanel>
                    <TextBlock Text="Thanh toán" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TotalText" FontSize="18" Margin="0,20,0,16"/>
                    <TextBlock Text="Phương thức"/>
                    <ComboBox x:Name="PaymentMethodComboBox" Margin="0,4,0,10"
                              SelectionChanged="PaymentMethodComboBox_SelectionChanged">
                        <ComboBoxItem Content="Tiền mặt tại quầy" Tag="Cash"/>
                        <ComboBoxItem Content="Chuyển khoản" Tag="Transfer"/>
                    </ComboBox>
                    <TextBlock x:Name="PaymentNoteText" Foreground="#667085" TextWrapping="Wrap"
                               FontSize="12" Margin="0,0,0,14"/>
                    <Button x:Name="CheckoutButton" Content="Gửi yêu cầu thanh toán"
                            Height="38" Click="CheckoutButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
