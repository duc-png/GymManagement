<UserControl x:Class="GymManagement.Views.MyCartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Giỏ hàng của tôi" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,14">
            <TextBlock Text="Thêm gói tập:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="PackageComboBox" Width="260" DisplayMemberPath="PackageName" SelectedValuePath="Id"/>
            <Button Content="Thêm vào giỏ" Width="110" Margin="8,0,0,0" Click="AddPackageButton_Click"/>
            <ComboBox x:Name="BookingComboBox" Width="260" DisplayMemberPath="StartTime" SelectedValuePath="Id" Margin="16,0,0,0"/>
            <Button Content="Thêm PT" Width="90" Margin="8,0,0,0" Click="AddBookingButton_Click"/>
            <Button Content="Xóa mục chọn" Width="110" Margin="8,0,0,0" Click="RemoveButton_Click"/>
        </StackPanel>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <DataGrid x:Name="CartGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,0,12,0">
                <DataGrid.Columns><DataGridTextColumn Header="Tên" Width="*" Binding="{Binding ItemName}"/><DataGridTextColumn Header="Loại" Width="100" Binding="{Binding ItemType}"/><DataGridTextColumn Header="Giá" Width="140" Binding="{Binding UnitPrice, StringFormat={}{0:N0} đ}"/></DataGrid.Columns>
            </DataGrid>
            <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Padding="16">
                <StackPanel>
                    <TextBlock Text="Thanh toán" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TotalText" FontSize="18" Margin="0,20,0,16"/>
                    <TextBlock Text="Phương thức"/><ComboBox x:Name="PaymentMethodComboBox" Margin="0,4,0,16"><ComboBoxItem Content="Tiền mặt" Tag="Cash"/><ComboBoxItem Content="Thẻ" Tag="Card"/><ComboBoxItem Content="Chuyển khoản" Tag="Transfer"/><ComboBoxItem Content="MoMo" Tag="MoMo"/></ComboBox>
                    <Button Content="Thanh toán giỏ hàng" Height="38" Click="CheckoutButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
